<form method="post" action="{{ path('app_recyclage_delete', {'id': recyclage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recyclage.id) }}">
    <button class="btn">Delete</button>
</form>
