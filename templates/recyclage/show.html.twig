 {% extends 'backComp.html.twig' %} 
{# 
{% block title %}Recyclage{% endblock %}

{% block main %}
    <h1>Recyclage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recyclage.id }}</td>
                <td>{{ recyclage.don }}</td>


            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recyclage_index') }}">back to list</a>

    <a href="{{ path('app_recyclage_edit', {'id': recyclage.id}) }}">edit</a>

    {{ include('recyclage/_delete_form.html.twig') }}
{% endblock %} #}

{% block title %}Recyclage{% endblock %}

{% block main %}
    <div class="container">
        <h1 class="my-4">Recyclage</h1>
        
        <table class="table table-bordered table-hover">
            <tbody>
                <tr>
                    <th scope="row">Id</th>
                    <td>{{ recyclage.id }}</td>
                    <td>{{ recyclage.don }}</td>
                </tr>
            </tbody>
        </table>

        <div class="btn-group my-3">
            <a href="{{ path('app_recyclage_index') }}" class="btn btn-primary">Back to list</a>
            <a href="{{ path('app_recyclage_edit', {'id': recyclage.id}) }}" class="btn btn-secondary">Edit</a>
            {{ include('recyclage/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}

