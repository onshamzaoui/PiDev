
<form method="post" action="{{ path('app_affectation_service_delete', {'id': affectation_service.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ affectation_service.id) }}">
    <button class="btn">Delete</button>
</form>
