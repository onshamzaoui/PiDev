<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>AffectationService Receipt</title>
		<style>
			/* Add your own styles here */
			.container {
				width: 600px;
				margin: 0 auto;
			}
			.receipt-header {
				text-align: center;
				margin-top: 50px;
				font-size: 2rem;
				font-weight: bold;
				color: #333;
			}
			.receipt-body {
				margin-top: 30px;
				padding: 20px;
				border: 1px solid #ccc;
				background-color: #f9f9f9;
				box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
			}
			.receipt-body p {
				margin: 10px 0;
				font-size: 1.2rem;
				color: #555;
			}
			.receipt-footer {
				text-align: center;
				margin-top: 30px;
				font-size: 1.5rem;
				font-style: italic;
				color: #999;
			}
		</style>
	</head>
	<body>
		<div class="container">
			<div class="receipt-header">AffectationService Receipt</div>
			<div class="receipt-body">
				<table class="table">
        			<tbody>
            			<tr>
                			<th>Id</th>
                			<td>{{ affectation_service.id }}</td>
            			</tr>
						<tr>
							<th>Date of Service</th>
							<td>{{ affectation_service.DateService ? affectation_service.DateService|date('Y-m-d') : '' }}</td>
						</tr>
						<tr>
							<th>Time of Service</th>
							<td>{{ affectation_service.HeureService ? affectation_service.HeureService|date('H:i:s') : '' }}</td>
						</tr>
						<tr>
							<th>Location of Service</th>
							<td>{{ affectation_service.LieuxService }}</td>
						</tr>
        			</tbody>
    			</table>
			</div>
			<div class="receipt-footer">Thank you for choosing our service!</div>
		</div>
	</body>
</html>