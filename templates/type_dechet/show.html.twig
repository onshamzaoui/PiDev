{% extends 'backComp.html.twig' %}

{% block title %}TypeDechet{% endblock %}

{% block main %}
    <h1>TypeDechet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_dechet.id }}</td>
            </tr>
            <tr>
                <th>NomDechet</th>
                <td>{{ type_dechet.NomDechet }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_dechet_index') }}">back to list</a>

    <a href="{{ path('app_type_dechet_edit', {'id': type_dechet.id}) }}">edit</a>

    {{ include('type_dechet/_delete_form.html.twig') }}
{% endblock %}
