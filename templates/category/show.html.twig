{% extends 'backComp.html.twig' %}

{% block title %}Category{% endblock %}

{% block main %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom Category</th>
                <td>{{ category.namecategory }}</td>
            </tr>
             <tr>
                <th>tauxReduction</th>
                <td>{{ category.tauxReduction }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_index') }}">back to list</a>

    <a href="{{ path('app_category_edit', {'id': category.id}) }}">edit</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
